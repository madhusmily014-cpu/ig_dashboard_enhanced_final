# Core
flask==3.1.3
requests==2.33.1
python-dotenv==1.2.2
gunicorn==21.2.0

# Cloudinary — video storage + auto-delete (Feature 4)
cloudinary==1.44.1

# NOTE: 'schedule' and 'threading' removed — scheduling is handled by GitHub Actions
